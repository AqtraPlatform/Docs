name: Markdown Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  MD_LINT_ENABLED: "1" # set "0" to skip markdown lint entirely

jobs:
  markdownlint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Use markdownlint-cli2 GitHub Action
      - name: Markdown lint (non-blocking)
        if: ${{ env.MD_LINT_ENABLED != '0' }}
        uses: DavidAnson/markdownlint-cli2-action@v16
        continue-on-error: true
        with:
          globs: |
            **/*.md
            !site/**
            !build/**
            !docs/assets/**
            !node_modules/**
            !.venv/**
          config: .markdownlint-cli2.jsonc

      # Uncomment to keep CI green while introducing lint gradually:
      # - name: Markdown lint (soft-fail)
      #   if: failure()
      #   run: echo "⚠️  Markdown lint warnings present — soft fail enabled"; exit 0
